<style type="text/css">
  .tinyStarField pre {
    font-family: monospace;
    font-size: 1.22em;
  }
</style>
<div class="tinyStarField">
  <h4>
    @{{ entry.handle }}
  </h4>
  <pre>{{ entry.tweet.text }}</pre>
  {{#unless last}}<hr />{{/unless}}
</div>
